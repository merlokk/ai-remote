# The ESP-IDF half of the repository. The root .gitignore covers the rest.

build/
managed_components/

# Generated by `idf.py set-target`, target-specific — sdkconfig.defaults is the
# committed one (CLAUDE.md §10.14.4).
sdkconfig
sdkconfig.old
