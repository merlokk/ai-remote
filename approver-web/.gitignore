node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Runtime config. Public material only — key custody ended up in the browser, so
# there is no private key here (CLAUDE.md, "Key custody"). Still per-machine
# state, not a repository fact: config.example.json is the committed reference.
config.json

# Agent skills: third-party markdown, restored from skills-lock.json (which IS
# committed) with `npx skills experimental_install`. See CLAUDE.md, "Skills".
.claude/skills/
