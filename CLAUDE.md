# Claude permission approver

The applied goal of the project is to move Claude Code's permission confirmation outside the terminal. Instead of the interactive permission prompt, the `PermissionRequest` hook sends a request into NATS (request-reply), an external human responder signs the decision with an Ed25519 key, and the hook verifies the signature and hands Claude Code an `allow`/`deny` verdict. Trusted responder keys are provisioned through a separate registration process using one-time tokens. The full protocol, message contracts, and fail-safe requirements live in [`approver/CLAUDE.md`](approver/CLAUDE.md) (§6–§7).

It runs on a local [NATS](https://nats.io/) sandbox on Docker Desktop under Windows: the whole infrastructure comes up with a single `docker compose` — see [`nats/CLAUDE.md`](nats/CLAUDE.md) (§3–§4).

## 1. Repository rules

- **TDD.** Test first, then code. The red → green → refactor cycle: write a failing test for the behavior → the minimal code to make it green → refactor under green tests. New functionality and bugfixes come together with tests; a PR without tests for the changed behavior is not merged. The test runner is **pytest**, tests live in `tests/`, files `test_*.py`.
- **List of libraries in use** (the only approved ones):
  - **Runtime:** `nats-py` (NATS client), `cryptography` (Ed25519 / ECDSA P-256 — sign/verify).
  - **Optional runtime:** `fido2` (YubiKey / CTAP2 / WebAuthn — the ARKG `previewSign` flow of §8). **Optional on purpose:** only `lib/yubikey.py`, `tools/yubikey_exec.py` and `approver/responder_yubikey.py` (§8.7) may need it — the **core approval path** (`hook.py` / `responder.py` / `registration_handler.py` / `protocol.py`) must never import it, so the §6/§7 flow keeps working on a bare `uv sync`. Declared in the `yubikey` extra, not in `[project.dependencies]`.
  - **Dev/tests:** `pytest`.
  - The Python standard library — no restrictions.
  - **Rust (`statusline/` only, §9.4).** `async-nats` (the official NATS client — the Rust counterpart of `nats-py`), `tokio` (`rt`/`time`/`macros` — `async-nats` is async and ships no runtime), `serde` + `serde_json` (the wire format, already inside `async-nats`'s tree), and `futures` **dev-only** (reading a subscription in the integration test). Declared in `statusline/Cargo.toml`, locked in `statusline/Cargo.lock`; the Rust standard library has no restrictions. None of this reaches the Python side — `uv sync` is unaffected.
  - **C++ / ESP-IDF (`approver-esp32/` and `approver-esp32-yubikey/`, §10) — approved.** The firmware is **C++**, with C only where a C ABI is forced, and **no dynamic memory** beyond one named exception (`std::string`): everything is allocated statically and lives for the life of the device (§10.14.1), built library layer first, logic second (§10.14.2). The dependency set is signed off — ESP-IDF and its in-tree components, cJSON, LVGL v9 + `espressif/esp_lvgl_port`, the panel and touch drivers, libsodium for Ed25519 (mbedTLS has no EdDSA and §6's server key is Ed25519 by fixed protocol), and `debsahu/espidf-nats` for the bus. **The list, every resolved version, the two transitive components nobody signed off in advance, and what each costs in bytes are [`approver-esp32/build.md`](approver-esp32/build.md) §10.4's**, which is also where the ESP-IDF version question is argued; declared in `main/idf_component.yml` and locked in `dependencies.lock` (committed), the way `uv.lock` and `Cargo.lock` pin the rest. Four host-side tools — `ffmpeg`, `lvgl-mcp-server`, `System.Drawing` and Unity — are on that machine rather than in the build, and are §10.4's last table. None of this reaches the Python, Rust or Node halves: `uv sync` is unaffected.

    **`approver-esp32-yubikey/` is that list minus four and plus one.** Gone with the panel: LVGL, `esp_lvgl_port` and the two display drivers. Added: **`espressif/usb`**, the USB Host Library — which is the only entry on either board's list that is not already signed off above, and it is Espressif's own code, in the framework's tree on v5.5.x and a managed component on the v6.0.2 installed here (the third dependency with that history, after cJSON and libsodium). It brings nothing transitive. `espressif/usb_host_hid` is deliberately **not** taken: a FIDO key is not a boot-protocol keyboard, CTAPHID is its own framing on two raw interrupt endpoints, and that framing is ~200 lines that folder owns and host-tests — taking the class driver would be a dependency added in order to not use most of it. [`approver-esp32-yubikey/build.md`](approver-esp32-yubikey/build.md) §10.4 has the argument and what it costs in bytes.
  - **Where they are declared.** The single source of truth is `pyproject.toml`: runtime in `[project.dependencies]`, optional runtime in `[project.optional-dependencies].yubikey`, dev/tests in `[dependency-groups].dev` (PEP 735). Versions are locked in `uv.lock` (committed). The project is non-package (`[tool.uv] package = false`, no `[build-system]`): we do not build a distribution, there is no editable install.
  - **Installation (uv):** `uv sync` (runtime + dev), `uv sync --no-dev` (runtime only), `uv sync --extra yubikey` (adds `fido2`). No `requirements*.txt` — they do not exist.
  - **Optional deps must degrade, not crash.** A module backed by an extra has to import cleanly when the extra is absent (lazy/guarded import + an `X_AVAILABLE` flag + a `require_X()` raising an actionable error), and its tests must skip rather than fail. `lib/yubikey.py` is the reference implementation of this pattern.
- **You may not pull in new libraries without confirmation.** Any dependency outside the list above (including transitive ones that drag in noticeable weight, and dev tools) is added only after explicit sign-off from the repository owner. The preference is to solve the task with the standard library. If a new dependency really is needed — ask first, then add it and update this list.

## 2. Structure

**Every folder documents itself.** This is the map; the per-folder files own the
protocol and API detail. Section numbers are **global and stable** — code
docstrings cite them (`CLAUDE.md §6`, `§8.4`), so a section keeps its number
wherever it lives.

| Path | What is in it | Its own docs |
|------|---------------|--------------|
| `approver/` | the approval flow: `registration_handler.py` (**the server** — owns the allowlist) and two clients, `responder.py` (software key) and `responder_yubikey.py` (key on a YubiKey); plus `hook.py` (the Claude Code side) and `protocol.py` (the shared wire format) | [`approver/CLAUDE.md`](approver/CLAUDE.md) — **§6** registration, **§7** the hook protocol, **§8.7** the YubiKey responder |
| `approver-web/` | a third client: the same responder as a web page (Next.js + Chakra UI — the Node half of the repo). It also reads the two watch-only subjects — the limits of §9.7 as a plaque, and what Claude is doing (§9.10) as one line under it | [`approver-web/CLAUDE.md`](approver-web/CLAUDE.md) |
| `approver-esp32/` | a fourth client, **answering**: the responder as firmware — ESP32-C6-Touch-AMOLED-2.16 on ESP-IDF, one press on a screen on the desk. It registers itself with the handler, takes requests off `approvals.*`, puts each on the glass for two buttons to answer, and signs and publishes the verdict the hook then trusts — a key bound to the chip, verified by `hook.verify_reply`. Around that: the hardware under it, seven screens over it (a clock, the limits of §9.7 with §9.10's activity line under them, the request card, the settings list and what is behind it), and a configuration site to open on a phone, with a password on the door and no way from it to a verdict. **What is real and what is not is that folder's own business, not this table's** — its `CLAUDE.md` opens with the short answer and [`status.md`](approver-esp32/status.md) is row by row; a status kept in three places is a status kept in none | [`approver-esp32/CLAUDE.md`](approver-esp32/CLAUDE.md) — **§10** the map, what this device is in the protocol (**§10.2**), the bus it needs on the LAN (**§10.3**) and the rules it may not soften (**§10.10**), with the rest split by subject and each section keeping its number: [`hardware.md`](approver-esp32/hardware.md) (**§10.1** the board, **§10.13** the parts with no job, **§10.14.3** the leased I²C bus), [`protocol.md`](approver-esp32/protocol.md) (**§10.5** the bus client, **§10.6** the key, **§10.7** registration and the console), [`firmware.md`](approver-esp32/firmware.md) (**§10.9** Wi-Fi, **§10.14** the language and the layering, **§10.15** the settings file), [`screens.md`](approver-esp32/screens.md) (**§10.8**), [`web.md`](approver-esp32/web.md) (**§10.16**), [`tests.md`](approver-esp32/tests.md) (**§10.11**), [`build.md`](approver-esp32/build.md) (**§10.4** the dependencies, **§10.12** the build and the size numbers) and [`status.md`](approver-esp32/status.md) (row by row, what is real yet); [`working-with-code.md`](approver-esp32/working-with-code.md) — where ESP-IDF is installed on this machine, how to get a shell that has `idf.py`, and how to photograph the panel; [`commands.md`](approver-esp32/commands.md) — every console command the device answers; and [`user-manual.md`](approver-esp32/user-manual.md) — the only document in that folder written for the person *using* the device rather than changing it: the buttons, every screen with a picture of it taken off the real panel ([`images/`](approver-esp32/images/)), first-time setup, the phone site, and the troubleshooting table |
| `approver-esp32-yubikey/` | a fifth client, and the one where the human is **not** a button: the responder as firmware on a **YD-ESP32-S3**, with a **security key on its USB OTG port**. Same subjects and the same allowlist as the board next door — what differs is **who signs**. This device holds no signing key of its own: it derives an ARKG public key from the authenticator's seed key (§8's `previewSign` flow, the one `approver/responder_yubikey.py` uses), registers *that* as a `key_type: "p256"` responder, and every verdict is an ECDSA signature the security key makes over the request's own bytes while somebody is touching it. A touch that cannot be replayed onto another request, a device with **no private key to steal**, and a key that cannot approve alone either — plus a `deny` that costs a touch as well, which is the one place this is more awkward than the button next door and its `key.md` says so. There is no screen — **one WS2812 is the whole user interface**, and which colour means what is compiled in rather than settable, because an operator who can recolour `denied` can build a device that lies about what it did. **What is real and what is not is that folder's own business, not this table's**: its `CLAUDE.md` opens with the short answer — the loop is closed — a real request has gone request → touch → signed reply → hook, and `hook.verify_reply` called the reply trusted, so this is a working responder and what is left is the request arriving from a live session rather than from the probe — and [`status.md`](approver-esp32-yubikey/status.md) is row by row | [`approver-esp32-yubikey/CLAUDE.md`](approver-esp32-yubikey/CLAUDE.md) — **§10** the map, sharing its numbering with `approver-esp32/` because it is **the same design on different hardware**: a comment citing `§10.14.2` means the same rule in either folder, and only where this device genuinely differs does a section get a new number. Two do: [`led.md`](approver-esp32-yubikey/led.md) (**§10.17** the one output — the palette, the ranking behind it, and the UART trick that drives a WS2812 with no extra component) and [`key.md`](approver-esp32-yubikey/key.md) (**§10.18** the security key — that it is the *signer*, the ARKG derivation in `components/arkg` that gives it a key to sign with and the three implementations it is pinned against, enrolment and what it now costs the registration, and the five checks an answer has to pass). One inherited section has diverged rather than been renumbered — **§10.6**, key custody, which is a question this board stopped answering when the security key became the signer. The rest keep their numbers and say what they inherit rather than repeating it: [`hardware.md`](approver-esp32-yubikey/hardware.md) (**§10.1**, **§10.13** — including the two USB-C sockets that are not interchangeable, which is the whole reason this board could do this), [`protocol.md`](approver-esp32-yubikey/protocol.md) (**§10.5**–**§10.7**), [`firmware.md`](approver-esp32-yubikey/firmware.md) (**§10.9**, **§10.14**, **§10.15**), [`web.md`](approver-esp32-yubikey/web.md) (**§10.16**), [`tests.md`](approver-esp32-yubikey/tests.md) (**§10.11**), [`build.md`](approver-esp32-yubikey/build.md) (**§10.4**, **§10.12**), [`status.md`](approver-esp32-yubikey/status.md), [`commands.md`](approver-esp32-yubikey/commands.md) and [`working-with-code.md`](approver-esp32-yubikey/working-with-code.md). **§10.16 is the one inherited section worth naming here rather than only there**: the configuration site is the sibling board's component ported rather than rewritten, so it keeps its number — and on a board with no glass and no keyboard it is not a convenience but the only way in that needs no cable. What it may never do is reach a verdict, which §10.10 rule 4 had been enforcing by there being no server at all and now enforces with a whitelist that refuses the gate's own settings by name. And two documents that are not part of that numbering at all. [`user-manual.md`](approver-esp32-yubikey/user-manual.md), the only one there written for whoever is **holding** the device rather than changing it — the two sockets that are not interchangeable, the one light, the touch that approves and the tap-then-touch that refuses, first-time setup, and a troubleshooting table keyed on what the light is doing, with three photographs of the real board in [`images/`](approver-esp32-yubikey/images/) for the states words are worst at. And [`README.md`](approver-esp32-yubikey/README.md), the short form for somebody arriving at the folder rather than working in it — what the device is, the four hardware facts, build and flash, first-time setup and what the light means. **It owns no section and decides nothing**, the way this repository's own `README.md` does not: every line in it summarises one of the files above, which stay authoritative |
| `lib/` | shared modules — `bus.py`, `config.py`, `crypto.py`, `yubikey.py`, and the pinned Yubico PEM bundles | [`lib/CLAUDE.md`](lib/CLAUDE.md) — **§8**–**§8.4**, **§8.6** (YubiKey / ARKG) |
| `tools/` | command-line utilities — `test_request.py` (probe a responder), `yubikey_exec.py` (the ARKG CLI) | [`tools/CLAUDE.md`](tools/CLAUDE.md) — **§8.5** |
| `scripts/` | Windows command files that drive the flows end to end | [`scripts/README.md`](scripts/README.md) — how to run each one; the rationale sits with the flow each drives (`e2e-registration` / `e2e-approval` / `yubikey-approval` in `approver/`, `test-request` / `yubikey-arkg` in `tools/`, `web-approval` in [`approver-web/`](approver-web/CLAUDE.md), `esp32-approval` / `esp32-host-tests` / `make-vectors` in [`approver-esp32/`](approver-esp32/CLAUDE.md) §10.11). `esp32yk-host-tests` is the same launcher for [`approver-esp32-yubikey/`](approver-esp32-yubikey/CLAUDE.md) §10.11 — two boards, two host tiers, two launchers, because a pre-commit check that has to be told which half of the repository it is checking is a check that gets run on one half. `esp32yk-make-vectors` is that folder's own tier-2 generator, and it is a *second* one rather than a flag on `make-vectors` because what it pins is not a shared protocol fact: the ARKG derivation of §10.18 exists on one of the two boards. That folder's **end-to-end** script is still owed and its [`tests.md`](approver-esp32-yubikey/tests.md) says so: it needs a security key that has not been plugged in yet. **All three of that folder's test tiers have a way in from here now**, which they did not: the host suite and the parity-vector generator were reachable only by knowing where they lived |
| `statusline/` | the Claude Code status line in Rust — the model plus how much of the 5h / 7d rate limits is spent, on screen and published to NATS. The same binary, wired as three hooks, also publishes what the session is *doing* — the tool about to run, the tool that just ran, the turn that ended | [`statusline/CLAUDE.md`](statusline/CLAUDE.md) — **§9** the line, **§9.7** the `status` subject, **§9.8** the connection dot, **§9.10** the `activity` subject |
| `nats/` | docker-compose: the NATS server, dashboard and `nats-box` (CLI) — plus `nats-server.conf`, which exists for the one setting that cannot be a flag (the 64 KB payload ceiling, a fail-safe for the ESP32) | [`nats/CLAUDE.md`](nats/CLAUDE.md) — **§3** the compose file and that config, **§4** NATS concepts the rest of the repo relies on |
| `tests/` | the pytest suite (`test_*.py`) and the `conftest.py` markers that keep it green with no Docker and no hardware | [`tests/CLAUDE.md`](tests/CLAUDE.md) |
| `screens/` | the screenshots `yubikey-hackaton.md` walks through: 1–6 are the YubiKey run step by step, and `7. web-approver screenshot.png` is the same loop through a browser tab, named at the end of that list as the second client rather than a seventh step. It is a **phase-1** shot — the badge says `unsigned`, from before the page signed — and that document says so where it links it. No docs of their own | — |

`lib/`, `approver/` and `tools/` have an `__init__.py` so `import lib.bus` /
`from approver import protocol` resolve; the scripts additionally prepend the
repo root to `sys.path` so they also work when run directly by path.

Project-level files:

- `README.md` — the long form, and the one a reader arriving at the repository lands on: what it is, how to run it, the smoke tests, and the command reference. Its scope is **the protocol and the two Python responders**, stated in its own opening: the other three components (`approver-web/`, `approver-esp32/`, `statusline/`) are named there with a link each and documented in their own folders rather than summarised twice — this table is the map, and a second copy of it in the README is a second copy to keep in step. `yubikey-hackaton.md` — the hackathon short form: the problem, the solution, and the one command that runs the whole loop. Both are for that arriving reader; the `CLAUDE.md` files are for working inside the repository, and are the ones to keep authoritative.
- `tasks.md` — **the cross-repository list of what is still open**: what every document says is owed, gathered in one place and checked against the code. Kept as a list rather than a ledger — an item is deleted when it is done, so the numbering has gaps and its own opening explains which. Two things follow from it living here. Its numbers are **not** part of §2's global scheme (§2 is this section), so cite them **with the file name** — `tasks.md §2.5`, never a bare `§2.5`, which reads as a subsection of this map; `approver-esp32/web.md` and `protocol.md` do exactly that. And it is not a second `approver-esp32/status.md`: that file is row-by-row about the firmware and stays authoritative for it.
- `pyproject.toml` — project metadata and dependencies (runtime + dev group); the source of truth for dependencies. In `[tool.pytest.ini_options]`: `pythonpath=["."]` (importing `lib.*` in a non-package project), `testpaths=["tests"]`, `--basetemp=.pytest_tmp` (the default temp root is unavailable in this sandbox).
- `uv.lock` — locked versions (uv), committed to the repository.
- `statusline-config.example.json` — the defaults for the status line's runtime config (§9.9). The live copy lives next to the built binary, not here; this is the committed record of the format, and a test asserts it matches the defaults.
- `.claude/settings.json` — committed, project-wide: it wires the status line to the built binary, and the same binary as the `PreToolUse` / `PostToolUse` / `Stop` hooks that publish the `activity` subject (§9.5, §9.10). `.claude/settings.local.json` is the per-machine half — granted permissions and the `PermissionRequest` hook (§7) — and is git-ignored. **There are three committed `settings.json` files, not one**: `approver-esp32/.claude/settings.json` and `approver-esp32-yubikey/.claude/settings.json` each carry a copy, because a session started inside either folder makes *it* the project root and the root file is then out of scope. The second of the two also carries the three `activity` hooks (§9.5, §9.10), which the first does not; that began as a device-specific reason and is no longer one — `approver-esp32-yubikey/` has no display and no longer watches that subject, and the readers left are `approver-web/` and the sibling board, neither of which cares which folder the publishing session was started in. The `.gitignore` entry below is unanchored across the same set of files. Both name this machine's absolute path to the binary, so it is two places to edit if it moves; that is the known cost of Claude Code having no repository-relative form for it, and it is why the hook's own entry stays in the git-ignored half rather than being *made* portable by committing it — the committed files are not portable either, they are merely harmless to a checkout that has no such binary.
- `.mcp.json` — committed, project scope: the one MCP server this repository registers, `lvgl-mcp-server`, which renders an LVGL screen on the host with no board attached (§10.12.1). Claude Code asks to approve it once per machine (`/mcp`). It is a design tool — nothing links against it and it is not on §1's dependency list.
- `.github/workflows/tests.yml` — CI, on every push and pull request, and it runs **tests only**: `pytest` against a NATS brought up from `nats/docker-compose.yml`, `cargo fmt --check` + `cargo test` against the same, `npm test` + `npm run build` (which is the TypeScript check), and the ESP32 host tier of §10.11 — the last on Windows, because that is the configuration its `CMakeLists.txt` is known to build under, and with Unity and cJSON cloned at the versions this project builds against rather than an ESP-IDF install. The two tiers that need hardware stay out: the device tier wants the board and a finger, and the browser tier wants a browser. Until this existed §1's TDD rule was enforced by whoever remembered to run four commands.
- `LICENSE` — the repository's licence, and the only file here nothing else cites.
- `.gitignore` — **the file itself is commented line by line with the reason for each rule; this bullet is not a second copy of it.** What is worth knowing without opening it: build output and caches are ignored, and so is every runtime config that holds a secret — `approver/responder-config.json`, `approver/handler-config.json`, `approver/responder-yubikey-config.json`, plus saved `yubikey-exec` credentials — whose `*.example.json` siblings carry no secrets and **are** committed. `/notes.txt` is a personal scratchpad; the `.claude/settings.local.json` pattern is unanchored on purpose. Three subfolders keep their own: `approver-esp32/` and `approver-esp32-yubikey/` for the two ESP-IDF halves (`sdkconfig` is generated, `sdkconfig.defaults` **is** committed, §10.14.4) and `approver-web/` for the Node half, including `.claude/skills/` — restored from the committed `skills-lock.json`.

> §3 (the infrastructure) and §4 (NATS concepts) moved to
> [`nats/CLAUDE.md`](nats/CLAUDE.md), keeping their numbers — `tests/conftest.py`
> and `statusline/src/nats.rs` cite them.

## 5. Python (host)
- Run via the **`py`** launcher (Python 3.14.6): `py script.py`, `py -m pytest`, `py -c "..."`.
- The real interpreter that `py` points to: `C:\Users\User\AppData\Local\Python\pythoncore-3.14-64\python.exe`.
  `C:\...\WindowsApps\python.exe` is the Microsoft Store stub, do NOT use it.
- **But the dependencies are in `.venv`, not in that interpreter**, and the line
  above is only true for code that imports nothing outside the standard library.
  `py` selects `.venv` only when `VIRTUAL_ENV` is set in the calling shell;
  without it, anything reaching `lib/bus.py` or `lib/crypto.py` — which is
  `hook.py`, both responders, the handler, `tools/`, and the whole pytest suite —
  fails with `ModuleNotFoundError`. So: activate the venv, or name the
  interpreter by path, `.venv\Scripts\python.exe`. Every script in `scripts/`
  resolves it that way and [`scripts/README.md`](scripts/README.md) says why.
